 <li>

    <%= link_to user.name, user %> <span> &nbsp; blah blah from _user file&nbsp; </span>
    <% if current_user.admin? && !current_user?(user) %>
       <%= link_to "edit not working", '#' %>
     | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
    
    <% end %>
 </li>